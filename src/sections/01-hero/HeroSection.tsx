import SectionContainer from '@/components/layouts/SectionContainer';

const HeroSection = () => {
  return <SectionContainer sectionId='home'>HeroSection</SectionContainer>;
};

export default HeroSection;
