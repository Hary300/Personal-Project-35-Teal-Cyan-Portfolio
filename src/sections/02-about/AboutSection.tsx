import SectionContainer from '@/components/layouts/SectionContainer';

const AboutSection = () => {
  return <SectionContainer sectionId='about'>AboutSection</SectionContainer>;
};

export default AboutSection;
