import SectionContainer from '@/components/layouts/SectionContainer';

const SkillSection = () => {
  return <SectionContainer sectionId='skill'>SkillSection</SectionContainer>;
};

export default SkillSection;
