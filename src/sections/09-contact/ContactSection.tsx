import SectionContainer from '@/components/layouts/SectionContainer';

const ContactSection = () => {
  return (
    <SectionContainer sectionId='contact'>ContactSection</SectionContainer>
  );
};

export default ContactSection;
